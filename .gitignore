# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/playwright-report
/test-results
/blob-report
/playwright/.cache

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (segredos nunca versionados; só .env.example entra no repo)
.env
.env*.local

# task workflow (não versionado)
task-brief.yaml

# typescript
*.tsbuildinfo

# vercel
.vercel

# dev-workflow (estado retomável de /task; nunca versionado)
.dev-workflow/state/

# design-sync machine state (durable inputs live in .design-sync/, committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
